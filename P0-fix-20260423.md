# P0 修复任务完成

## P0-1: README GLM 免费示例错误

**项目**: oh-my-coder (`~/.qclaw/workspace-agent-bf627e2b/projects/oh-my-coder/`)

**问题**: README 中 `GLM_API_KEY=free` 示例会导致 401 报错（GLM 免费模型需要真实 API Key）

**修复**:
- L71: `omc config set -k GLM_API_KEY -v "free"` → `omc config set -k GLM_API_KEY -v "your_key"  # 注册获取：https://open.bigmodel.cn/`
- L184: 同上修复

**Commit**: `545de2b`

**验证**: `grep -n GLM_API_KEY README.md` 显示两处已修复为 your_key + 注册链接