# 2026-04-24 日志

## P1-3 模型元数据系统
- 创建 `src/models/model_metadata.json`（7 production + 5 beta + 1 deprecated）
- 创建 `src/models/metadata.py`（`get_models_by_status()` / `get_all_models()`）
- `src/cli_model.py` 重构：默认只显示 production，`--all` 显示全部+状态标签，`--beta` 只显示 beta
- 桌面端 `ModelSelector.tsx`：隐藏 beta 模型，搜索模式 bypass 过滤，"其他模型(N个)" 展开按钮
- 所有改动已 push

## 🚨 重大教训：双 Git 仓库问题
workspace 根目录（`workspace-agent-bf627e2b/`）和 `projects/oh-my-coder/` 是两个独立 git 仓库指向同一远程。
本地 oh-my-coder 仓库有 114 个 commit 从未 push 过（新版 README、Skill 系统、Agent 交叉验证等），
远程停在旧版本。已经 force push 恢复。桌面端 ModelSelector 文件后来单独补 push。

## CI 修复
- `src/commands/quickstart.py:567` 的 `str(e)` → 替换为 `"请求失败"`
- commit `af04e52`，已 push

## 双仓库状态
- workspace 仓库：`af04e52`
- oh-my-coder 子仓库：已同步到 `af04e52`，全部一致
- 两个仓库均指向 `github.com/VOBC/oh-my-coder.git`
